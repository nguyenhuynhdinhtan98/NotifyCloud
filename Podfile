# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PushNotifyCloud' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Firebase'
  pod 'FirebaseCore'
  pod 'FirebaseMessaging'
  # Pods for PushNotifyCloud

  target 'PushNotifyCloudTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PushNotifyCloudUITests' do
    # Pods for testing
  end

end
